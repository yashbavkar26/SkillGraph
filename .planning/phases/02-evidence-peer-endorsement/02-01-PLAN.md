---
phase: 02-evidence-peer-endorsement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/types/evidence.ts, src/api/evidence.ts, src/components/evidence/EvidenceLinker.tsx]
autonomous: true
requirements: [FR-2, FR-10]

must_haves:
  truths:
    - "User can submit an external URL as evidence for a specific skill"
    - "Evidence is correctly persisted in Neo4j and linked to both User and Skill"
    - "Evidence can be retrieved for a specific user via API"
  artifacts:
    - path: "src/types/evidence.ts"
      provides: "Evidence type definitions"
    - path: "src/api/evidence.ts"
      provides: "Evidence CRUD endpoints (Express)"
    - path: "src/components/evidence/EvidenceLinker.tsx"
      provides: "UI for linking evidence"
  key_links:
    - from: "src/components/evidence/EvidenceLinker.tsx"
      to: "/api/evidence"
      via: "fetch POST"
      pattern: "fetch.*\\/api\\/evidence"
    - from: "src/api/evidence.ts"
      to: "Neo4j database"
      via: "neo4j-driver Cypher query"
      pattern: "session\\.run.*CREATE\\s+\\(e:Evidence\\)"
---

<objective>
Implement the ability for users to link external artifacts (URLs, etc.) as evidence for their skills.

Purpose: Provides verifiable data points to strengthen the user's skill graph.
Output: Evidence API, Evidence types, and Evidence Linking UI component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/DOMAIN_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Evidence interfaces and Neo4j schema logic</name>
  <files>src/types/evidence.ts</files>
  <action>Create TypeScript interfaces for Evidence (id, userId, skillId, url, type, metadata). Include a Zod schema for validation. Note: In Neo4j, this will be a node with labels :Evidence and edges :HAS_EVIDENCE to User and :DEMONSTRATES to Skill.</action>
  <verify>
    <automated>ts-node src/types/evidence.ts (if it contains a test/example)</automated>
  </verify>
  <done>Evidence interface and Zod schema exported</done>
</task>

<task type="auto">
  <name>Task 2: Implement Evidence Express API</name>
  <files>src/api/evidence.ts</files>
  <action>Create POST /api/evidence and GET /api/evidence/:userId endpoints. 
Use neo4j-driver to:
- POST: CREATE (u:User {id: $userId})-[:HAS_EVIDENCE]->(e:Evidence {id: $id, url: $url, type: $type})-[:DEMONSTRATES]->(s:Skill {id: $skillId})
- GET: MATCH (u:User {id: $userId})-[:HAS_EVIDENCE]->(e:Evidence) RETURN e
Validate input using the Zod schema from Task 1.</action>
  <verify>
    <automated>npm test -- --grep "evidence api" (if tests exist) or curl -X POST http://localhost:3000/api/evidence ...</automated>
  </verify>
  <done>Endpoints functional and persisting to Neo4j</done>
</task>

<task type="auto">
  <name>Task 3: Build EvidenceLinker UI Component</name>
  <files>src/components/evidence/EvidenceLinker.tsx</files>
  <action>Create a React component that allows a user to:
1. Select a skill from a dropdown (or passed via props).
2. Enter a URL.
3. Select a type (e.g., 'github', 'portfolio', 'certificate').
4. Submit the evidence.
Include loading and success/error states.</action>
  <verify>
    <automated>npm test -- --grep "EvidenceLinker" (if tests exist)</automated>
  </verify>
  <done>Component renders and successfully calls the API</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | {untrusted input for URL and skillId} |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I | /api/evidence | mitigate | Validate URL format and skillId existence using Zod and Cypher checks. |
| T-02-02 | S | /api/evidence | mitigate | Ensure userId is derived from auth context (not passed in body). |
</threat_model>

<verification>
- Verify evidence persistence in Neo4j via browser/cli.
- Verify UI correctly handles error states (e.g., invalid URL).
</verification>

<success_criteria>
- User can link a valid URL to a skill.
- The link appears in the user's evidence list.
- API returns 201 for success and 400 for invalid data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-evidence-peer-endorsement/02-01-SUMMARY.md`
</output>
