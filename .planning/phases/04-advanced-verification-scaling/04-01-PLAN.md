---
phase: 04-advanced-verification-scaling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/services/reputation/reputationService.ts, src/services/reputation/eigenTrust.ts, src/db/cypher/reputationQueries.ts, tests/unit/reputation/reputation.test.ts]
autonomous: true
requirements: [R4-01]
user_setup:
  - service: neo4j
    why: "Advanced reputation algorithms (EigenTrust) often require the Graph Data Science (GDS) library for efficient graph traversals and centrality calculations."
    dashboard_config:
      - task: "Ensure GDS plugin is installed and active in your Neo4j instance."
        location: "Neo4j Desktop/Server -> Plugins"

must_haves:
  truths:
    - "Reputation scores are calculated based on structural trust (EigenTrust/Contextual weighting)."
    - "Endorsements from high-reputation users have a mathematically higher impact on the recipient's score."
    - "Updated scores are successfully persisted to the User nodes in Neo4j."
  artifacts:
    - path: "src/services/reputation/reputationService.ts"
      provides: "Orchestration of reputation updates"
    - path: "src/services/reputation/eigenTrust.ts"
      provides: "Core mathematical weighting logic"
    - path: "src/db/cypher/reputationQueries.ts"
      provides: "Parameterized Cypher queries for reputation updates"
  key_links:
    - from: "src/services/reputation/reputationService.ts"
      to: "src/services/reputation/eigenTrust.ts"
      via: "direct method call"
      pattern: "eigenTrust\\.calculate"
    - from: "src/services/reputation/reputationService.ts"
      to: "src/db/cypher/reputationQueries.ts"
      via: "database driver call"
      pattern: "reputationQueries\\.updateScore"
---

<objective>
Implement the core Advanced Reputation Engine using EigenTrust or Contextual Weighting algorithms.

Purpose: To move beyond simple endorsement counting to a system where trust is a structural property of the graph, mitigating Sybil attacks and low-quality endorsements.
Output: A functional reputation calculation service and updated database schema/queries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/DOMAIN_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement core weighting and EigenTrust logic</name>
  <files>src/services/reputation/eigenTrust.ts, tests/unit/reputation/reputation.test.ts</files>
  <action>Implement the mathematical logic for EigenTrust or a highly sophisticated contextual weighting algorithm. The logic should be able to take a graph topology (as an adjacency list or similar structure) and return updated trust weights. Focus on the math: $Trust(A \to B) = Weight(A) \times Confidence(A, B)$. Use TDD to ensure the math holds for known test topologies (e.g., a star, a ring, and a cluster).</action>
  <verify>
    <automated>npm test -- tests/unit/reputation/reputation.test.ts --grep eigenTrust</automated>
  </verify>
  <done>Logic correctly calculates scores for complex test topologies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Reputation Service and Cypher integration</name>
  <files>src/services/reputation/reputationService.ts, src/db/cypher/reputationQueries.ts</files>
  <action>Create the `ReputationService` to orchestrate the calculation and `reputationQueries` to persist results. Use parameterized Cypher queries to update the `reputationScore` property on `User` nodes. Ensure the service can handle large-scale updates gracefully (e.g., batching updates if necessary).</action>
  <verify>
    <automated>npm test -- tests/unit/reputation/reputation.test.ts --grep reputationService</automated>
  </verify>
  <done>Service successfully orchestrates calculation and updates the database via parameterized queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API $\to$ ReputationService | Untrusted scoring requests (should be internal only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-REP-01 | I | Reputation Logic | mitigate | Use mathematically proven EigenTrust/Contextual weighting to prevent easy score manipulation. |
| T-REP-02 | T | Cypher Queries | mitigate | Use strictly parameterized queries to prevent Cypher injection. |
</threat_model>

<verification>
Ensure the reputation scores in Neo4j reflect the structural changes made in the test environment.
</verification>

<success_criteria>
The reputation service correctly calculates and persists scores that reflect the graph's trustworthiness, passing all unit and integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-verification-scaling/04-01-PLAN-SUMMARY.md`
</output>
