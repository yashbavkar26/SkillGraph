---
phase: 04-advanced-verification-scaling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/api/assessment/ingest.ts, src/services/assessment/ingestionService.ts, src/db/cypher/assessmentQueries.ts, tests/integration/assessment-ingestion.test.ts]
autonomous: true
requirements: [R4-02]
user_setup: []

must_haves:
  truths:
    - "External assessment data (e.g., JSON payloads) can be ingested via API."
    - "Assessment data is validated against a strict schema before ingestion."
    - "Assessment results are correctly linked to the corresponding User and Skill nodes in the graph."
  artifacts:
    - path: "src/api/assessment/ingest.ts"
      provides: "Ingestion API endpoint"
    - path: "src/services/assessment/ingestionService.ts"
      provides: "Data parsing and validation logic"
    - path: "src/db/cypher/assessmentQueries.ts"
      provides: "Graph linkage queries"
  key_links:
    - from: "src/api/assessment/ingest.ts"
      to: "src/services/assessment/ingestionService.ts"
      via: "service call"
      pattern: "ingestionService.process"
    - from: "src/services/assessment/ingestionService.ts"
      to: "src/db/cypher/assessmentQueries.ts"
      via: "database call"
      pattern: "assessmentQueries.linkAssessment"
---

<objective>
Implement the pipeline for ingesting and integrating automated assessment data into the SkillGraph.

Purpose: To allow external verifiable assessments (e.g., from certification bodies or technical tests) to automatically influence a user's skill profile.
Output: A functional ingestion API and graph linkage mechanism.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Assessment Ingestion API and Validation</name>
  <files>src/api/assessment/ingest.ts, src/services/assessment/ingestionService.ts</files>
  <action>Create a POST endpoint `/api/assessment/ingest` that accepts assessment data (User ID, Skill ID, score, timestamp, source). Use Zod to enforce a strict schema for the incoming payload. The service should handle the parsing and initial validation logic.</action>
  <verify>
    <automated>npm test -- tests/integration/assessment-ingestion.test.ts --grep api</automated>
  </verify>
  <done>API endpoint accepts valid payloads and rejects malformed ones with 400 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Graph Linkage and Persistence</name>
  <files>src/db/cypher/assessmentQueries.ts, tests/integration/assessment-ingestion.test.ts</files>
  <action>Implement the logic to link assessment data to the graph. This involves creating `Assessment` nodes and creating relationships (e.g., `(User)-[:HAS_ASSESSMENT]->(Assessment)-[:VALIDATES]->(Skill)`). Use parameterized Cypher queries to ensure data integrity and prevent injection.</action>
  <verify>
    <automated>npm test -- tests/integration/assessment-ingestion.test.ts</automated>
  </verify>
  <done>Successful ingestion results in correctly linked nodes and relationships in Neo4j.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Source $\to$ Ingestion API | Untrusted assessment data from third parties |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ASS-01 | I | Data Integrity | mitigate | Use strict Zod schema validation and cryptographic signatures (if source provides them) to ensure data authenticity. |
| T-ASS-02 | T | Cypher Injection | mitigate | Use strictly parameterized Cypher queries for all graph updates. |
</threat_model>

<verification>
Verify that a single ingestion call results in the expected number of new nodes and relationships in the graph.
</verification>

<success_criteria>
Automated assessments can be successfully ingested and correctly reflected in the user's skill profile within the graph.
</success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-verification-scaling/04-02-PLAN-SUMMARY.md`
</output>
