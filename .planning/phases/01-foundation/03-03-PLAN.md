---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/api/relationships/route.ts, tests/integration/graph.test.ts, src/types/graph.ts]
autonomous: true
requirements: [FR-1, TC-1]

must_haves:
  truths:
    - "User and Skill can be connected via a relationship edge"
    - "The relationship edge persists in the graph"
    - "An end-to-end integration test passes"
  artifacts:
    - path: "src/api/relationships/route.ts"
      provides: "Relationship management (User-Skill)"
    - path: "tests/integration/graph.test.ts"
      provides: "Full flow verification"
  key_links:
    - from: "src/api/relationships/route.ts"
      to: "src/db/neo4j.ts"
      via: "driver.session()"
      pattern: "driver.session"
---

<objective>
Implement the relationship management API and run end-to-end integration tests.

Purpose: Connect the core entities (User and Skill) to form the first meaningful subgraph.
Output: Working relationship API and a passing integration test suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/02-02-PLAN.md
@src/db/neo4j.ts
@src/types/graph.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement User-Skill Relationship API</name>
  <files>src/api/relationships/route.ts, src/types/graph.ts</files>
  <action>
1. Add `Relationship` interface to `src/types/graph.ts`.
2. Implement `src/api/relationships/route.ts` (POST to create a `HAS_SKILL` edge between a User and a Skill).
3. Ensure the API validates that both the User and the Skill exist before creating the edge.
</action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/relationships -d '{"userId": "...", "skillId": "..."}'</automated>
  </verify>
  <done>Users can be successfully linked to skills in the graph.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Integration Test</name>
  <files>tests/integration/graph.test.ts</files>
  <action>
Create a comprehensive integration test that:
1. Seeds a User and a Skill.
2. Creates a relationship between them.
3. Queries the graph to verify the relationship exists and contains the correct properties.
4. Cleans up the test data.
</action>
  <verify>
    <automated>npm test tests/integration/graph.test.ts</automated>
  </verify>
  <done>The full lifecycle of entity creation and relationship establishment is verified via automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Relationship creation payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | Graph Traversal | accept | Complexity limits on relationship creation. |
</threat_model>

<verification>
- Verify relationship creation via `curl`.
- Run integration tests.
- Check Neo4j browser/CLI to see the `(:User)-[:HAS_SKILL]->(:Skill)` pattern.
</verification>

<success_criteria>
A functioning graph where users are connected to skills, verified by integration tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/03-03-SUMMARY.md`
</output>
