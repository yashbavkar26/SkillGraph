---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/api/users/route.ts, src/api/skills/route.ts, src/models/user.ts, src/models/skill.ts, src/types/graph.ts]
autonomous: true
requirements: [FR-1, TC-1]

must_haves:
  truths:
    - "User API returns 201 upon successful creation"
    - "Skill API returns 201 upon successful creation"
    - "Fetching a user/skill by ID returns the correct data from Neo4j"
  artifacts:
    - path: "src/api/users/route.ts"
      provides: "User management endpoints"
    - path: "src/api/skills/route.ts"
      provides: "Skill management endpoints"
    - path: "src/models/user.ts"
      provides: "User data mapping"
    - path: "src/models/skill.ts"
      provides: "Skill data mapping"
  key_links:
    - from: "src/api/users/route.ts"
      to: "src/db/neo4j.ts"
      via: "driver.session()"
      pattern: "driver.session"
    - from: "src/api/skills/route.ts"
      to: "src/db/neo4j.ts"
      via: "driver.session()"
      pattern: "driver.session"
---

<objective>
Implement the core RESTful API endpoints and models for User and Skill entities.

Purpose: Enable the creation and management of the two primary node types in the graph.
Output: Working API endpoints for User and Skill management.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/DOMAIN_RESEARCH.md
@.planning/phases/01-foundation/01-01-PLAN.md
@src/db/neo4j.ts
@src/db/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement User Entity API</name>
  <files>src/models/user.ts, src/api/users/route.ts, src/types/graph.ts</files>
  <action>
1. Define `User` interface in `src/types/graph.ts`.
2. Implement `src/models/user.ts` with methods for `create` and `findById` using Cypher.
3. Implement `src/api/users/route.ts` (POST for creation, GET for retrieval).
</action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/users -d '{"email": "test@example.com", "name": "Test User"}'</automated>
  </verify>
  <done>User creation and retrieval works via API.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Skill Entity API</name>
  <files>src/models/skill.ts, src/api/skills/route.ts, src/types/graph.ts</files>
  <action>
1. Define `Skill` interface in `src/types/graph.ts`.
2. Implement `src/models/skill.ts` with methods for `create` and `findById` using Cypher.
3. Implement `src/api/skills/route.ts` (POST for creation, GET for retrieval).
</action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/skills -d '{"name": "TypeScript"}'</automated>
  </verify>
  <done>Skill creation and retrieval works via API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input for User/Skill creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Injection | Cypher Query | mitigate | Use parameterized queries in all Neo4j driver calls. |
| T-02-02 | Spoofing | User Creation | accept | Identity verification (auth) is scoped to a later phase. |
</threat_model>

<verification>
- Test POST /api/users with valid and invalid (duplicate email) payloads.
- Test POST /api/skills with valid and invalid (duplicate name) payloads.
- Verify data persistence in Neo4j via `cypher-shell`.
</verification>

<success_criteria>
Functional User and Skill management endpoints that persist data to Neo4j.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/02-02-SUMMARY.md`
</output>
