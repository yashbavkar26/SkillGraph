---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, src/db/neo4j.ts, src/db/schema.ts]
autonomous: true
requirements: [TC-1]
user_setup:
  - service: neo4j
    why: "Graph database for storing skills and relationships"
    env_vars:
      - name: NEO4J_PASSWORD
        source: "Set in docker-compose.yml or environment"

must_haves:
  truths:
    - "Neo4j container is running and reachable"
    - "Database driver can establish a connection"
    - "Initial schema constraints (e.g., uniqueness for User email) are applied"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Neo4j infrastructure"
    - path: "src/db/neo4j.ts"
      provides: "Neo4j driver instance"
    - path: "src/db/schema.ts"
      provides: "Cypher constraint definitions"
  key_links:
    - from: "src/db/neo4j.ts"
      to: "Neo4j Container"
      via: "neo4j-driver connection"
      pattern: "driver.session()"
---

<objective>
Set up the Neo4j graph database infrastructure and the core database client.

Purpose: Provide the foundational data layer required for all subsequent graph operations.
Output: Running Neo4j instance and a TypeScript database client.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/DOMAIN_RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Neo4j Infrastructure</name>
  <files>docker-compose.yml</files>
  <action>Create a docker-compose.yml file to run Neo4j with APOC plugin enabled. Include environment variables for NEO4J_AUTH (user/password).</action>
  <verify>
    <automated>docker compose up -d && docker compose ps</automated>
  </verify>
  <done>Neo4j container is running and healthy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DB Client and Schema Constraints</name>
  <files>src/db/neo4j.ts, src/db/schema.ts</files>
  <action>
1. Create `src/db/neo4j.ts` using `neo4j-driver` to export a singleton driver instance.
2. Create `src/db/schema.ts` containing a function `initializeSchema()` that runs Cypher commands to ensure uniqueness on User(email) and Skill(name).
3. Export an `initDb()` function that connects, runs schema initialization, and verifies connectivity.
</action>
  <verify>
    <automated>npx ts-node src/db/schema.ts</automated>
  </verify>
  <done>Driver instance exported and schema constraints verified via successful connection and execution of constraint commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> Neo4j | Connection using credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Neo4j Credentials | mitigate | Use environment variables for authentication (not hardcoded). |
</threat_model>

<verification>
- Verify Neo4j is accessible via `bolt://localhost:7687`.
- Verify schema constraints can be queried via Cypher `SHOW CONSTRAINTS`.
</verification>

<success_criteria>
A running Neo4j instance and a verified database connection/schema setup.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
